<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">
    <type name="Magento\Framework\App\RouterList">
        <arguments>
            <argument name="routerList" xsi:type="array">
                <item name="epepushengine" xsi:type="array">
                    <item name="class" xsi:type="string">Exotic\PushEngine\Controller\Router</item>
                    <!-- Lower sortOrder than the default "standard" router (20) so this
                         only intercepts the three literal asset paths it knows about and
                         falls through to normal CMS/catalog routing for everything else. -->
                    <item name="sortOrder" xsi:type="string">15</item>
                </item>
            </argument>
        </arguments>
    </type>
</config>

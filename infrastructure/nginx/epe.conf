# HTTP — redirect all traffic to HTTPS
server {
  listen 80;
  server_name example.com;
  return 301 https://$host$request_uri;
}

# HTTPS — main server block
server {
  listen 443 ssl;
  http2 on;
  server_name example.com;

  # TLS certificate paths — replace with actual paths (e.g. Let's Encrypt via certbot)
  ssl_certificate     /etc/ssl/certs/example.com.crt;
  ssl_certificate_key /etc/ssl/private/example.com.key;

  ssl_protocols       TLSv1.2 TLSv1.3;
  ssl_ciphers         HIGH:!aNULL:!MD5;
  ssl_prefer_server_ciphers on;
  ssl_session_cache   shared:SSL:10m;
  ssl_session_timeout 10m;

  # Security headers
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
  add_header X-Content-Type-Options    nosniff                                always;
  add_header X-Frame-Options           DENY                                   always;
  add_header X-XSS-Protection          "1; mode=block"                        always;
  add_header Referrer-Policy           "strict-origin-when-cross-origin"      always;

  # Dashboard's own BFF routes (Next.js route handlers under /api/dashboard/*).
  # This MUST be matched before the generic /api/ block below — both are literal
  # prefixes, and nginx always picks the longest matching prefix, so this is safe
  # regardless of file order, but keep it above for readability.
  location /api/dashboard/ {
    proxy_pass         http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
  }

  # Real backend API (NestJS, global prefix "api"). The trailing /api/ on proxy_pass
  # is intentional — NestJS expects requests at /api/..., not /..., so the prefix
  # must NOT be stripped on the way through.
  location /api/ {
    proxy_pass         http://127.0.0.1:3001/api/;
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
  }

  # Dashboard (Next.js) — everything else
  location / {
    proxy_pass         http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header   Upgrade           $http_upgrade;
    proxy_set_header   Connection        "upgrade";
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
  }
}
